server {
    listen 80;
    root /usr/share/nginx/html;

    location = /voice/ {
        try_files /voice/index.html =404;
    }

    location /voice/static/ {
        expires 1y;
        add_header Cache-Control "public, immutable";
        try_files $uri =404;
    }
}
